{
  "name": "demo-package",
  "version": "1.0.0",
  "module": "src/module.js",
  "scripts": {
    "transpile": "babel src -d dist/lib",
    "prepublish-pkg": "polypackage src --root=dist --out-dir=lib",
    "publish-pkg": "npm publish dist"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.13.2",
    "polypackage-cli": "*"
  },
  "private": true
}
